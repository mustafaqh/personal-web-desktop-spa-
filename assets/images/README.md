# Legacy wallpaper images (not used)

Files in this folder (`body1.jpg`, `body2.jpg`, `body3.jpg`, `body4.png`, etc.) come from an earlier Web OS prototype.

The Personal Web Desktop app does **not** load them. The live wallpaper is defined in `style/main.css` using CSS gradients only.

These files are kept for reference only and may be removed in a future cleanup if no longer needed.
